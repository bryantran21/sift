# dependencies
node_modules/

# secrets — never commit connection strings or keys
.env
.env.local
.env*.local

# next.js
/.next/
/out/
*.tsbuildinfo

# build / logs / os
dist/
# note: src/data/logos.json IS committed so favicons ship to production
# (Vercel doesn't run `pnpm build:logos`). Regenerate + recommit when sources change.

# personal: your résumé and its score never get committed
resume.txt
resume-score.json
*.log
npm-debug.log*
pnpm-debug.log*
.DS_Store
.vercel
/coverage
